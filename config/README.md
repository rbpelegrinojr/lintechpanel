# Configuration

This directory contains version-controlled, non-secret configuration examples and schemas.

Runtime configuration and secrets belong under `/etc/lintech-panel/` on Ubuntu. Never commit generated passwords, tokens, private keys, customer environment variables, or a production `.env` file.

