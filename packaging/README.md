# Packaging

Distribution metadata and package-building assets belong here when Debian or other supported packages are introduced. Version 0.1.0 is installed through `installer/install.sh`; no binary package is currently published.

